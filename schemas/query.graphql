type Query {
  appName: String!
  me: User!
  # get all users
  users: [User]!
  user(id: ID!): User!
  # get all experts
  experts: [User]!
  expert(id: ID!): User!

  # course
  courses: [Course]!
  course(id: ID!): Course!

  # tutorial
  tutorials: [Tutorial]!

  # practice
  practice(id: ID!): Practice!
  practices: [Practice]!

  # get uploaded files
  uploads: [File]
}
