type Query {
  appName: String!
  me: User!
  # get all users
  users: [User]!
  user(id: ID!): User!

  # course
  courses: [Course]!
  course(id: ID!): Course!

  # tutorial
  tutorials: [Tutorial]!

  # get uploaded files
  uploads: [File]
}
