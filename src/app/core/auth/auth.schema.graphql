mutation AuthLogin($data: LoginInput!) {
  login(data: $data)
}
mutation AuthRegister($data: RegisterInput!) {
  register(data: $data)
}

query AuthCurrentUser {
  me {
    id
    name
    role
    level
  }
}
