fragment UserFields on User {
  id
  name
  role
  level
  introduction
  avatar
}

query AdminUsers {
  users {
    ...UserFields
    createDate
    updateDate
  }
}

query Experts {
  experts {
    ...UserFields
  }
}
